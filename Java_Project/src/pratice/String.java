package pratice;

public class String {

	
	// charAT(int index) 지정된 인덱스 문자 반환
	// compareTo(String anotherString) 사전적 순서로 문자열 비교, 앞-1, 같0, 뒤1
	// concat(String str) 주어진 문자열을 현재열 뒤에 붙임
	// equals(Object anObject) 주어진 객채와 현재의 문자열 비교
	// equalsIgnoreCase(String anotherString) 대소문자 무시 비교
	// length() 문자열 길이반환
	// replace(char oldChar, char newChar) old char를 new char로 변경한, 새로운 문자열 생성해 반환
	// substring(int beginIndex, int endIndex) 현재 문자열 일부 반환
	// toLowerCase() 소문자로 변경
	// toUpperCase() 대문자로 변경
	
	
	// public 아무나, 공동의 재산
	// protected 자기 가족만, 본인package와 자식 class
	// private 나의 개인적인공간, 클래스 내에서만
	// 없음  자기 클래스와 패키니 안에서만 (default)
	
	// getters 값 가져오기, setters 정하기
}
